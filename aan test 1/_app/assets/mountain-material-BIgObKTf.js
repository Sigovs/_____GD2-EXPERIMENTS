import{A as e,C as t,D as n,V as r,j as i,z as a}from"./GLTFLoader-Cdku5qF-.js";var o={version:`1.0.0`,approved:`2026-08-19`,sourceGlb:`./models/mountain-lab/mountain-lab.glb`,mountains:[{node:`SM_Mountain_06`,role:`background`,x:-2600,y:0,z:-14200,yaw:-15,scale:4.4},{node:`SM_Iceland_Plateus_01`,role:`background`,x:4200,y:0,z:-15e3,yaw:-40,scale:4.6},{node:`SM_Iceland_Crest`,role:`background`,x:-7600,y:0,z:-13600,yaw:30,scale:5},{node:`SM_Mountain_Plateu_01`,role:`background`,x:7800,y:0,z:-13e3,yaw:25,scale:4},{node:`SM_Mountain_07`,role:`background`,x:-5e3,y:0,z:-12e3,yaw:-8,scale:3.8},{node:`SM_Mountain_05`,role:`background`,x:5600,y:0,z:-11400,yaw:12,scale:3.4},{node:`SM_Mountain_01`,role:`background`,x:-6400,y:0,z:-9200,yaw:40,scale:3},{node:`SM_Iceland_Mountain_02`,role:`background`,x:6800,y:0,z:-9600,yaw:-32,scale:3.2},{id:`destination-summit`,role:`midground`,src:`./models/mountain-lab/summit-peak.glb`,node:`SM_Summit_Peak`,keepMaterial:!0,x:340,y:0,z:-8400,yaw:96,scale:34,seatAfterScale:!0,bury:190},{id:`hero-shoulder-l`,node:`SM_Mountain_07`,role:`midground`,x:-1250,y:0,z:-7850,yaw:24,scale:2.6,seatAfterScale:!0,bury:1180},{id:`hero-shoulder-r`,node:`SM_Mountain_09`,role:`midground`,x:2150,y:0,z:-8250,yaw:-50,scale:2.2,seatAfterScale:!0,bury:980},{id:`hero-shoulder-front`,node:`SM_Iceland_Crest`,role:`midground`,x:900,y:0,z:-7350,yaw:62,scale:3,seatAfterScale:!0,bury:520},{id:`shoulder-a`,node:`SM_Mountain_01`,role:`foreground`,x:-1850,y:0,z:-5700,yaw:70,scale:2.3,seatAfterScale:!0,bury:900},{id:`shoulder-b`,node:`SM_Mountain_05`,role:`foreground`,x:2050,y:0,z:-4900,yaw:-35,scale:2.1,seatAfterScale:!0,bury:830},{id:`shoulder-c`,node:`SM_Iceland_Eroded_Mountain`,role:`foreground`,x:-2100,y:0,z:-3800,yaw:18,scale:2.4,seatAfterScale:!0,bury:940},{id:`shoulder-d`,node:`SM_Mountain_02`,role:`foreground`,x:2e3,y:0,z:-2850,yaw:-60,scale:2,seatAfterScale:!0,bury:800},{id:`shoulder-e`,node:`SM_Mountain_09`,role:`foreground`,x:-1900,y:0,z:-1900,yaw:40,scale:2.2,seatAfterScale:!0,bury:860},{id:`near-a`,node:`SM_Iceland_Crest`,role:`foreground`,x:-1050,y:0,z:-4450,yaw:12,scale:2,seatAfterScale:!0,bury:640},{id:`near-b`,node:`SM_Mountain_06`,role:`foreground`,x:1150,y:0,z:-3350,yaw:-44,scale:1.9,seatAfterScale:!0,bury:690},{id:`near-c`,node:`SM_Iceland_Plateus_01`,role:`foreground`,x:-1100,y:0,z:-2350,yaw:55,scale:2.2,seatAfterScale:!0,bury:560},{id:`shoulder-f`,node:`SM_Mountain_Plateu_01`,role:`foreground`,x:2200,y:0,z:-1050,yaw:-20,scale:2.4,seatAfterScale:!0,bury:940}],roleOrder:[`background`,`midground`,`foreground`],mobileOverrides:{SM_Iceland_Eroded_Mountain:{x:-850,z:-300,scale:1.3},SM_Mountain_02:{x:950,z:-550,scale:1.1}},camera:{near:1,far:6e4,baseFov:36,referenceAspect:16/9,maxAdaptedFov:50,introStart:{pos:[180,420,640],tgt:[55,465,-2800],fov:30},track:[{p:0,pos:[-130,880,-6250],tgt:[-148,340,-8384],fov:34},{p:.3,pos:[-300,560,-5250],tgt:[-90,690,-7300],fov:36},{p:.65,pos:[260,350,-3450],tgt:[60,520,-5700],fov:38},{p:1,pos:[-140,285,-1500],tgt:[40,430,-4300],fov:40}],mobileIntroStart:{pos:[80,380,800],tgt:[-9,380,-2800],fov:50},mobileTrack:[{p:0,pos:[-120,900,-6100],tgt:[-148,155,-8384],fov:50},{p:.3,pos:[-50,1060,-4950],tgt:[-40,700,-7250],fov:50},{p:.65,pos:[40,690,-2950],tgt:[55,390,-5850],fov:50},{p:1,pos:[10,360,-1350],tgt:[20,0,-4300],fov:50}]},scene:{background:`#9fbcd6`,sky:{top:`#4a7fb5`,horizon:`#cfdeeb`,exponent:.68,radius:3e4},fog:{color:`#cfdeeb`,near:1400,far:13500,introFar:7e3},hemisphere:{sky:14149883,ground:9413558,intensity:2.35},key:{color:16774376,intensity:4.3,azimuth:52,elevation:21,distance:2e4},floor:{size:34e3,color:14345970,y:-6,bury:120,footCap:210,footReach:1.45}},intro:{totalSeconds:3.4,background:{zOffset:-1400,duration:2.2,ease:`power2.out`,at:0,fadeDuration:1.7,fadeEase:`power1.inOut`,fadeAt:.15},midground:{xOffsetMagnitude:1500,duration:2.4,ease:`power3.out`,at:.25},foreground:{yOffset:-900,duration:2.5,ease:`power3.out`,at:.35},fog:{fromFar:6e3,toFar:13500,duration:2.6,ease:`power2.out`,at:0},approach:{duration:3.2,ease:`power2.inOut`,at:0},text:{at:.55,duration:1.5,stagger:.14,ease:`power3.out`,rotateY:-42,rotateX:12,z:-420,y:26},firstMarker:{duration:.7,ease:`power2.out`,at:1.5}},scroll:{sectionVh:400,scrub:1,pinSpacing:!1,parallax:{backgroundZ:900,midgroundZ:380},markers:[{id:`ml-marker-0`,hide:.16},{id:`ml-marker-1`,show:.34,hide:.6},{id:`ml-marker-2`,show:.78}]}};function s(e={}){return JSON.stringify({...o,...e},null,2)}var c={snowLine:40,snowTransition:230,slopeThreshold:.2,slopeSoftness:.36,rockScale:.0021,snowScale:.0032,normalStrength:.78,rockRoughness:.9,snowRoughness:.84,rockSaturation:.18,rockCool:1,snowBoost:1.4,snowNoiseAmp:150,snowNoiseScale:.0016,macroVariation:.24,macroScale:22e-5},l=`
// Anchored projection basis. vTriPos is NOT the live world position: it is the
// vertex in the mesh's own space, scaled to world units and offset by the
// mountain's REST anchor. That makes it invariant to any translation applied
// afterwards - scroll parallax, intro motion, group moves - so the texture
// stays glued to the geometry instead of sliding under it.
varying vec3 vTriPos;
varying vec3 vTriLocalNormal;
varying vec3 vTriWorldNormal;

uniform sampler2D uRockAlbedo;
uniform sampler2D uRockNormal;
uniform sampler2D uSnowAlbedo;
uniform sampler2D uSnowNormal;

uniform float uSnowLine;
uniform float uSnowTransition;
uniform float uSlopeThreshold;
uniform float uSlopeSoftness;
uniform float uRockScale;
uniform float uSnowScale;
uniform float uNormalStrength;
uniform float uRockRoughness;
uniform float uSnowRoughness;
uniform float uSnowNoiseAmp;
uniform float uSnowNoiseScale;
uniform float uMacroVariation;
uniform float uMacroScale;
uniform float uRockSaturation;
uniform float uRockCool;
uniform float uSnowBoost;

// Bound per mesh in onBeforeRender, because all 12 mountains share one material.
uniform vec3 uTriAnchor;
uniform float uTriScale;

float triHash(vec3 p) {
  p = fract(p * 0.3183099 + vec3(0.71, 0.113, 0.419));
  p *= 17.0;
  return fract(p.x * p.y * p.z * (p.x + p.y + p.z));
}

float triNoise(vec3 x) {
  vec3 i = floor(x);
  vec3 f = fract(x);
  f = f * f * (3.0 - 2.0 * f);
  return mix(
    mix(mix(triHash(i + vec3(0,0,0)), triHash(i + vec3(1,0,0)), f.x),
        mix(triHash(i + vec3(0,1,0)), triHash(i + vec3(1,1,0)), f.x), f.y),
    mix(mix(triHash(i + vec3(0,0,1)), triHash(i + vec3(1,0,1)), f.x),
        mix(triHash(i + vec3(0,1,1)), triHash(i + vec3(1,1,1)), f.x), f.y), f.z);
}

// Two octaves is enough to break a straight line without costing much.
float triFbm(vec3 p) {
  return triNoise(p) * 0.65 + triNoise(p * 2.17) * 0.35;
}

vec3 triBlendWeights(vec3 n) {
  vec3 w = abs(n);
  // Sharpened so the three projections do not smear into each other.
  w = pow(w, vec3(4.0));
  return w / max(w.x + w.y + w.z, 1e-5);
}

vec4 triSample(sampler2D tex, vec3 p, vec3 w, float scale) {
  vec4 xz = texture2D(tex, p.zy * scale);
  vec4 yz = texture2D(tex, p.xz * scale);
  vec4 xy = texture2D(tex, p.xy * scale);
  return xz * w.x + yz * w.y + xy * w.z;
}

// Whiteout blend: keeps detail from all three planes instead of letting the
// dominant axis flatten the other two.
vec3 triSampleNormal(sampler2D tex, vec3 p, vec3 wn, vec3 w, float scale, float strength) {
  vec3 nx = texture2D(tex, p.zy * scale).xyz * 2.0 - 1.0;
  vec3 ny = texture2D(tex, p.xz * scale).xyz * 2.0 - 1.0;
  vec3 nz = texture2D(tex, p.xy * scale).xyz * 2.0 - 1.0;
  nx.xy *= strength;
  ny.xy *= strength;
  nz.xy *= strength;
  vec3 sn = sign(wn);
  nx = vec3(nx.xy + wn.zy, abs(nx.z) * wn.x);
  ny = vec3(ny.xy + wn.xz, abs(ny.z) * wn.y);
  nz = vec3(nz.xy + wn.xy, abs(nz.z) * wn.z);
  return normalize(nx.zyx * w.x + ny.xzy * w.y + nz.xyz * w.z);
}

// Height + slope, with noise folded into the height term so the transition is
// ragged rather than a level white line across every peak.
float triSnowMask(vec3 worldPos, vec3 worldNormal) {
  float n = triFbm(worldPos * uSnowNoiseScale) - 0.5;
  float h = worldPos.y + n * 2.0 * uSnowNoiseAmp;
  float byHeight = smoothstep(uSnowLine - uSnowTransition, uSnowLine + uSnowTransition, h);
  float upness = clamp(worldNormal.y, 0.0, 1.0);
  float bySlope = smoothstep(uSlopeThreshold - uSlopeSoftness,
                             uSlopeThreshold + uSlopeSoftness, upness);
  return clamp(byHeight * bySlope, 0.0, 1.0);
}
`;function u(e,n={}){let i={...c,...n},a=new t({map:e.rockAlbedo,normalMap:e.rockNormal,metalness:0,roughness:i.rockRoughness,fog:!0}),o={uRockAlbedo:{value:e.rockAlbedo},uRockNormal:{value:e.rockNormal},uSnowAlbedo:{value:e.snowAlbedo},uSnowNormal:{value:e.snowNormal},uSnowLine:{value:i.snowLine},uSnowTransition:{value:i.snowTransition},uSlopeThreshold:{value:i.slopeThreshold},uSlopeSoftness:{value:i.slopeSoftness},uRockScale:{value:i.rockScale},uSnowScale:{value:i.snowScale},uNormalStrength:{value:i.normalStrength},uRockRoughness:{value:i.rockRoughness},uSnowRoughness:{value:i.snowRoughness},uSnowNoiseAmp:{value:i.snowNoiseAmp},uSnowNoiseScale:{value:i.snowNoiseScale},uMacroVariation:{value:i.macroVariation},uMacroScale:{value:i.macroScale},uRockSaturation:{value:i.rockSaturation},uRockCool:{value:i.rockCool},uSnowBoost:{value:i.snowBoost},uTriAnchor:{value:new r(0,0,0)},uTriScale:{value:1}};return a.onBeforeCompile=e=>{Object.assign(e.uniforms,o),e.vertexShader=e.vertexShader.replace(`#include <common>`,`#include <common>
varying vec3 vTriPos;
varying vec3 vTriLocalNormal;
varying vec3 vTriWorldNormal;
uniform vec3 uTriAnchor;
uniform float uTriScale;`).replace(`#include <worldpos_vertex>`,`#include <worldpos_vertex>
  // Object space scaled into world units, then offset by the rest anchor.
  // Translation of the mesh or any parent group does not enter this value.
  vTriPos = transformed * uTriScale + uTriAnchor;
  vTriLocalNormal = normalize(objectNormal);
  vTriWorldNormal = normalize(mat3(modelMatrix) * objectNormal);`),e.fragmentShader=e.fragmentShader.replace(`#include <common>`,`#include <common>\n${l}`).replace(`#include <map_fragment>`,`
  // Blend weights come from the LOCAL normal so the three projection planes
  // rotate with the mountain - that is what "glued" means. The snow mask still
  // uses the world normal, so snow keeps sticking to world-up faces.
  vec3 triW = triBlendWeights(vTriLocalNormal);
  float triSnow = triSnowMask(vTriPos, vTriWorldNormal);
  vec3 triRockCol = triSample(uRockAlbedo, vTriPos, triW, uRockScale).rgb;
  vec3 triSnowCol = triSample(uSnowAlbedo, vTriPos, triW, uSnowScale).rgb;

  // Rock: pull toward its own luminance, then tilt cold. Every rock map in
  // this pack is warm yellow; this is what makes it read as slate.
  float triRockLum = dot(triRockCol, vec3(0.2126, 0.7152, 0.0722));
  triRockCol = mix(vec3(triRockLum), triRockCol, uRockSaturation);
  triRockCol *= vec3(1.0 - uRockCool * 0.055, 1.0 - uRockCool * 0.012, 1.0 + uRockCool * 0.075);
  // Rock reads as charcoal at these light levels; lift it into slate so it is a
  // secondary material rather than a black hole next to the snow.
  triRockCol *= 1.28;

  triSnowCol *= uSnowBoost;

  vec3 triCol = mix(triRockCol, triSnowCol, triSnow);
  // Macro variation is applied to ROCK only: brightness noise across snow is
  // exactly what reads as dirt.
  float triMacro = triFbm(vTriPos * uMacroScale);
  float triMacroAmt = uMacroVariation * (1.0 - triSnow);
  triCol *= mix(1.0 - triMacroAmt, 1.0 + triMacroAmt, triMacro);
  diffuseColor.rgb *= triCol;
`).replace(`#include <roughnessmap_fragment>`,`
  float roughnessFactor = mix(uRockRoughness, uSnowRoughness, triSnow);
`).replace(`#include <normal_fragment_maps>`,`
  vec3 triRockN = triSampleNormal(uRockNormal, vTriPos, vTriWorldNormal, triW, uRockScale, uNormalStrength);
  vec3 triSnowN = triSampleNormal(uSnowNormal, vTriPos, vTriWorldNormal, triW, uSnowScale, uNormalStrength * 0.7);
  normal = normalize(mix(triRockN, triSnowN, triSnow));
`),a.userData.shader=e},a.userData.uniforms=o,a.userData.params=i,a.customProgramCacheKey=()=>`triplanar-mountain-v1`,a}function d(e,t){let n=e.userData.uniforms;if(n)for(let[r,i]of Object.entries({snowLine:`uSnowLine`,snowTransition:`uSnowTransition`,slopeThreshold:`uSlopeThreshold`,slopeSoftness:`uSlopeSoftness`,rockScale:`uRockScale`,snowScale:`uSnowScale`,normalStrength:`uNormalStrength`,rockRoughness:`uRockRoughness`,snowRoughness:`uSnowRoughness`,snowNoiseAmp:`uSnowNoiseAmp`,snowNoiseScale:`uSnowNoiseScale`,macroVariation:`uMacroVariation`,macroScale:`uMacroScale`,rockSaturation:`uRockSaturation`,rockCool:`uRockCool`,snowBoost:`uSnowBoost`}))t[r]!==void 0&&n[i]&&(n[i].value=t[r],e.userData.params[r]=t[r])}function f(t=`./models/mountain-lab/materials/`){let n=new a,r=(r,i)=>new Promise((a,o)=>{n.load(t+r,t=>{t.wrapS=e,t.wrapT=e,t.colorSpace=i,t.anisotropy=4,a(t)},void 0,o)});return Promise.all([r(`rock_albedo.jpg`,i),r(`rock_normal.jpg`,``),r(`snow_albedo.jpg`,i),r(`snow_normal.jpg`,``)]).then(([e,t,n,r])=>({rockAlbedo:e,rockNormal:t,snowAlbedo:n,snowNormal:r}))}function p(e,t){e.updateWorldMatrix(!0,!1);let i=new r,a=new n,o=new r;e.matrixWorld.decompose(i,a,o),e.userData.triAnchor=i.clone(),e.userData.triScale=o.x,e.onBeforeRender=(n,r,i,a,o)=>{let s=o&&o.userData&&o.userData.uniforms||t.userData.uniforms;!s||!s.uTriAnchor||(s.uTriAnchor.value.copy(e.userData.triAnchor),s.uTriScale.value=e.userData.triScale)}}export{d as a,f as i,p as n,o,u as r,s,c as t};